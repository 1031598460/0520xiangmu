import { combineReducers} from 'redux'

function f(prevState={},action) {
    switch (action.type) {
        default:
            return prevState;
    }
}
export default combineReducers({
    f
})